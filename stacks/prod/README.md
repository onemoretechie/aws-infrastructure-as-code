# stack: prod

Production-grade composition of the modules in this repository.

**Status:** planned. Will mirror the `dev` stack with stricter defaults (NAT-per-AZ, flow logs enabled, multi-AZ RDS, etc.).
